{
    "name": "@buildingai/desktop",
    "private": true,
    "version": "0.1.0",
    "type": "module",
    "scripts": {
        "prepare": "node scripts/prepare-frontend.js",
        "dev": "node scripts/prepare-frontend.js && tauri dev",
        "dev:desktop": "node scripts/prepare-frontend.js && tauri dev",
        "build:desktop": "node scripts/prepare-frontend.js && tauri build",
        "tauri": "tauri"
    },
    "dependencies": {
        "@tauri-apps/api": "^2",
        "@tauri-apps/plugin-opener": "^2"
    },
    "devDependencies": {
        "@tauri-apps/cli": "^2"
    }
}